import { Blockquote } from "@/components/shared/Blockquote";
import CodeBlock from "@/components/shared/Codeblock";
import Demo from "@/app/docs/environment-and-lifecycle/useIsClient/Demo";
import Table from "@/components/shared/Table";
import data from "@/app/docs/environment-and-lifecycle/useIsClient/data.json";

export const metadata = {
  title: 'useIsClient - vibeHooks',
  description: 'Returns true if the code is running in the browser and false if it is running on the server.',
  openGraph: {
    title: 'useIsClient - vibeHooks',
    description: 'Returns true if the code is running in the browser and false if it is running on the server.',
  }
}

# useIsClient

### Returns true if the code is running in the browser and false if it is running on the server.

<Blockquote.Note>
  <Blockquote.Content>This hook is essential for preventing hydration mismatch errors in SSR frameworks like Next.js or Remix. Use it to conditionally render components that rely on browser-only APIs.</Blockquote.Content>
</Blockquote.Note>

#### Installation

<CodeBlock
  code={`npm install @vibehooks/react/useIsClient`}
  language="bash"
/>

#### Description

The **useIsClient** is essential for SSR (Server-Side Rendering) environments like Next.js or Remix. It ensures that components requiring browser-only APIs (like **window** or **document**) only execute logic or render specific UI elements after the component has hydrated on the client, preventing hydration mismatch errors.

#### Return values

<Table data={data.returnValues} />

#### Demo

<Demo />

#### Source code

```tsx
'use client';

import { useIsClient } from "@vibehooks/react/useIsClient";
import { Tag } from "@/components/ui/Tag";
import { Layout } from "@/layouts/Layout";

export default function Demo() {
  const isClient = useIsClient();
	return (
		<Layout
    >
      <Layout.Title>Is Client?</Layout.Title>
      <div className="flex flex-col items-center gap-1.5">
        <Layout.Caption>Current environment:</Layout.Caption>
        <Tag.Primary>{isClient ? 'Client-side' : 'Server-side'}</Tag.Primary>
      </div>
		</Layout>
	);
}
```
